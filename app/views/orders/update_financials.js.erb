// Updates the DOM with taxes and total values
$('#subtotal').text('<%= number_to_currency(@order.subtotal) %>');
$('#tax').text('<%= number_to_currency(@order.tax) %>');
$('#total').text('<%= number_to_currency(@order.total) %>');
